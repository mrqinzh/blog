<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mrqinzh.blog.mapper.MyMessageMapper">
    <cache />

    <insert id="add" parameterType="MyMessage">
        insert into t_message(avatar, nickname, content, `time`,  ip) values (#{avatar}, #{nickname}, #{content}, #{time}, #{ip})
    </insert>

</mapper>