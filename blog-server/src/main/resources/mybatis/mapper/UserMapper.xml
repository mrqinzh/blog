<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mrqinzh.blog.mapper.UserMapper">

    <cache />

    <insert id="add" parameterType="com.mrqinzh.blog.model.entity.User">
        INSERT INTO `user` (
            role_name,
            login_last_time,
            user_avatar,
            user_nickname,
            user_email,
            user_name,
            user_pwd
        )
        VALUES
        (
            #{roleName}, #{loginLastTime}, #{userAvatar}, #{userNickname}, #{userEmail}, #{userName}, #{userPwd}
        )
    </insert>

    <select id="list" resultType="com.mrqinzh.blog.model.entity.User">
        SELECT
            id, role_name, login_last_time, user_avatar, user_nickname, user_email, user_name
        FROM
            `user`
    </select>

</mapper>